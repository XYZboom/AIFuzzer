run:
  description: "Search for generated invalid code"
  seed: 12345
  output_dir: "./reports"

generator:
  min_nodes_per_graph: 3
  max_nodes_per_graph: 12
  min_inputs: 2
  max_inputs: 6
  min_input_ndim: 1
  max_input_ndim: 4

backends:
  enabled: ["tvm"]

  tvm:
    python: "/home/xyzboom/Programs/miniconda3/envs/aifuzzer/bin/python3"
    mode: "daemon"                     # 注意: "process" 已废弃，每次 import tvm ~5s

pipeline:
  workers: 1
  batch_size: 200
  run_timeout_seconds: 30
