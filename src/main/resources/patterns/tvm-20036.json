{
  "format_version": "1.0",
  "patterns": [
    {
      "id": "tvm-20036",
      "compiler": "tvm",
      "target": "cuda",
      "description": "batch_norm CUDA storage_rewrite buffer_red before declaration — shape[0]=1, shape[-1]=1",
      "severity": "crash",
      "nodes": [
        {
          "match": "node",
          "id": "n0",
          "op": "BATCH_NORM",
          "inputs": ["v_in"],
          "outputs": ["v_out"]
        }
      ],
      "values": [
        {
          "match": "value",
          "id": "v_in",
          "ndim": 3,
          "shape": [
            {"$eq": 1},
            {},
            {"$eq": 1}
          ],
          "dtype": "float32"
        }
      ]
    }
  ]
}